module.exports = {
  createChart: () => ({
    addLineSeries: () => ({ setData: ()=>{} }),
    timeScale: { fitContent: ()=>{} },
    applyOptions: ()=>{},
    remove: ()=>{},
    resize: ()=>{},
  }),
}
